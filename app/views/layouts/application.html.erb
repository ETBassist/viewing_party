<!DOCTYPE html>
<html>
  <head>
    <title>ViewingParty</title>
    <%= csrf_meta_tags %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center">
            <a href="/" class="text-green-600 px-3 py-2 rounded-md text-2xl font-extrabold">Viewing Party</a>

            <div class="ml-2 flex items-baseline space-x-4">
              <a href="/discover" class="text-green-600 hover:bg-green-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Discover Movies</a>
            </div>
          </div>

          <% if current_user %>
            <div class="ml-4 flex items-center space-x-4 md:ml-6">
              <p class="text-green-600 text-sm font-light">Welcome, <%= current_user.name %>!</p>
              <%= link_to "Logout", "/logout", class: "text-green-600 hover:bg-green-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>

              <a href="/dashboard" class="bg-green-600 hover:bg-green-700 text-white px-3 py-2 rounded-md text-sm font-medium">Dashboard</a>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <% flash.each do |type, message| %>
    <p><%= message %></p>
    <% end %>

    <main>
      <%= yield %>
    </main>
    
    <footer>
      <img src="https://www.themoviedb.org/assets/2/v4/logos/v2/blue_square_2-d537fb228cf3ded904ef09b136fe3fec72548ebc1fea3fbbd1ad9e36364db38b.svg" alt="The MovieDB Logo" width="75px" style="margin:10px" >
      <div>
        <p>
          Thank you to <a href="https://www.themoviedb.org/about/logos-attribution">The MovieDB</a> for providing the movie data that is used in this application.
        </p>
        <p>
          Further citation information regarding the movie data used in this project can be found <a href="http://files.grouplens.org/datasets/movielens/ml-latest-small-README.html">here.</a>
        </p>
      </div>
    </footer>
  </body>
</html>
